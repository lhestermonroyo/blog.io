import AUTH_STATE from './auth';
import POST_STATE from './post';
import TAG_STATE from './tag';
import NOTIFICATION_STATE from './notification';

const states = {
  auth: AUTH_STATE,
  post: POST_STATE,
  tag: TAG_STATE,
  notification: NOTIFICATION_STATE
};

export default states;
