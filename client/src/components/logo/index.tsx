import React, { FC } from 'react';
import { Text } from 'native-base';

const Logo: FC = () => {
  return (
    <Text fontSize="xl" fontWeight="medium">
      <Text color="primary.700">forum</Text>.io
    </Text>
  );
};

export default Logo;
