import React, { FC } from 'react';
import { Text } from 'native-base';

import PrivateLayout from '../../layout/private-layout';

const SubForums: FC = () => {
  return (
    <PrivateLayout>
      <Text>SubForums Page</Text>
    </PrivateLayout>
  );
};

export default SubForums;
